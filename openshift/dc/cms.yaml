apiVersion: v1
kind: DeploymentConfig
metadata:
  labels:
    app: fun-platform-cms
  name: fun-platform-cms
spec:
  replicas: 1  # number of pods we want
  selector:
    app: fun-platform-cms
    deploymentconfig: fun-platform-cms
  template:
    metadata:
      labels:
        app: fun-platform-cms
        deploymentconfig: fun-platform-cms
    spec:
      containers:
      - env:
        - name: SERVICE_VARIANT
          value: cms
        - name: DJANGO_SETTINGS_MODULE
          value: cms.envs.docker_run
        image: docker-registry.default.svc:5000/campus-dev/fun-platform-edxapp:whitebrand
        imagePullPolicy: Always
        name: fun-platform-cms
        volumeMounts:
        - mountPath: /config
          name: volume-config
        - mountPath: /data/media
          name: edxapp-media
        - mountPath: /data/static
          name: edxapp-static
      volumes:
      - name: volume-config
        configMap:
          defaultMode: 420
          name: config
      - name: edxapp-media
        persistentVolumeClaim:
          claimName: media
      - name: edxapp-static
        persistentVolumeClaim:
          claimName: static
